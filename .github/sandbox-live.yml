# Sandbox live run config — PR #111: ADP throttle resilience + ADP-aware retry-dates.
#
# full-live: exercises the refactored _wait_for_login_form against real ADP (happy path:
# step=uid-box-visible log line proves goto-based recovery still lands on the login SPA).
# Evidence needed to lift Claude review confidence from 88% to ≥95%.
scenarios:
  - name: full-live
    date: "2026-06-28"
