# Sandbox live run config — OFF.
#
# Add scenarios here + apply the `sandbox-live` label to a PR to run a real
# (isolated) BHAGA scrape on the PR image; remove them once evidence is captured.
# An empty/absent scenarios list means nothing runs.
#
# Last used: PR #58 (otp-reprompt, 2026-06-14) — proved BHAGA_OTP_FORCE_REQUEST
# force re-prompt on real bhaga-sandbox-refresh Cloud Run + Firestore (sandbox_runs):
# seeded stale checkpoint, ran gate-only (no scrape/OTP reply), verified
# requested_at advanced + ready_received=False. Evidence in PR #58 §4.
scenarios: []
