# Sandbox live run config — list scenarios to run on the next push. Empty = off.
# Issue #343's evidence run was triggered by PR comment (`/sandbox run full-live
# date=2026-09-24`), so nothing is armed here.
scenarios: []
