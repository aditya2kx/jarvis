# Sandbox live run config — ACTIVE for fix/https-github-com-aditya2kx-jarvis-issues (PR #99).
#
# Proves the inline-autostart OTP gate default on real Cloud Run + Firestore.
# Runs the nightly-autostart gate-only scenario (no scrape, no OTP reply needed)
# to verify otp_gate.evaluate returns PROCEED immediately with no BHAGA_OTP_ASSUME_READY
# and no BHAGA_OTP_REQUIRE_READY set, and that no pending_otp checkpoint is written.
#
# REMOVE this file's scenarios (set to []) after evidence is captured.
#
# Last used: PR #99 (fix/https-github-com-aditya2kx-jarvis-issues, 2026-06-28)
scenarios:
  - name: nightly-autostart
    date: "2026-06-27"
