# Sandbox live run config — PR #58 otp-reprompt evidence capture.
#
# Proving the BHAGA_OTP_FORCE_REQUEST=1 re-prompt fix on the real
# bhaga-sandbox-refresh Cloud Run job + Firestore (sandbox_runs). Gate-only:
# no scrape, no OTP reply needed. Will be reverted to scenarios: [] once
# evidence lands in the PR comment and is pasted into PR #58 §4.
scenarios:
  - name: otp-reprompt
    date: 2026-06-14
