# Sandbox live run config for PR #41 — Square login resilience proof.
#
# Runs the "item-sales-live" scenario against the isolated sandbox for the
# incident date (2026-06-08): a REAL Square login + transactions/item-sales/KDS
# scrape on the PR branch's image. This exercises the new anti-bot recovery path
# end-to-end:
#   • normal/SMS-OTP or deliverable magic link → login succeeds, verify(item_sales)
#     proves item-sales rows landed in the sandbox (no regression to the happy path);
#   • blank-recipient soft-block → _magic_link_recipient raises SquareDeviceBlockedError,
#     the poisoned session is discarded and login retries ONCE fresh, and on a hard
#     block it fails cleanly via square_device_blocked_alert (no dead-end paste).
# Full isolation: reads prod, writes ONLY sandbox; any OTP/2FA is labeled [SANDBOX].
#
# Remove this file (or empty the scenarios list) once PR evidence is captured.
scenarios:
  - name: item-sales-live
    date: "2026-06-08"
