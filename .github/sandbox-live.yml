# Idle. Live sandbox is opt-in: add the `sandbox-live` label AND list scenarios
# here for that PR only. Empty this back to `scenarios: []` before merge.
# It is not a required check — do not leave a leftover full-live date armed.
scenarios: []
