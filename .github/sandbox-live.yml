# Sandbox live run config — ACTIVE for fix/debug-and-fix-issues-with-bhaga.
#
# Proves the ADP earnings ready-dialog fix on the real ADP portal with the PR
# image. Runs the full-live scenario (Square + ADP + model) against sandbox
# targets for 2026-06-23 (the date of the failing nightly). Evidence in PR §4.
# REMOVE this file's scenarios (set to []) before merge — captured once is enough.
#
# Operator: answer the [SANDBOX] ADP OTP prompt in Slack DM D0B67MW6J02.
# Expected log lines: [earnings] step=download-report-button-ready → step=downloaded
#
# Last used: PR # (fix/debug-and-fix-issues-with-bhaga, 2026-06-25) — proves ADP
# earnings ready-dialog wait fix (BHAGA_ADP_EARNINGS_READY_TIMEOUT_MS=90s, fallbacks).
scenarios: []
# Evidence captured — see PR #72 §4.3 for verbatim Cloud Run logs.
