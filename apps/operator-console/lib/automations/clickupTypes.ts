/** Shared ClickUp automation types/constants (safe for client components). */

/** Single Palmetto ClickUp workspace — not shown in the UI. */
export const DEFAULT_WORKSPACE_ID = "9017956545";

export type ClickUpNamedOption = { id: string; label: string };
